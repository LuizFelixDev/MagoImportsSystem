# URL de conexão com o banco de dados PostgreSQL (pode ser obtido no painel da Vercel / Neon / Supabase)
POSTGRES_URL="postgresql://usuario:senha@host:port/database?sslmode=require"

# Chave secreta para assinar os tokens JWT (digite uma frase secreta forte aqui)
JWT_SECRET="magoimports_jwt_secret_key_2026"

# Porta onde o servidor Express rodará localmente (padrão é 2020 se não definido)
PORT=2020